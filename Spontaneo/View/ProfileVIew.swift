import SwiftUI

struct ProfileView: View {
    
    var body: some View {
        HeaderView()
    
    }
}

struct ProfileView_Preview: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
